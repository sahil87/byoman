#!/bin/bash
# Quick idea capture - append to .specify/todo.md
set -e

if [ $# -eq 0 ]; then
  echo "Usage: todo <your idea>" >&2
  exit 1
fi

echo "- $(date +%Y-%m-%d): $*" >> "$(git rev-parse --show-toplevel)/.specify/todo.md"
